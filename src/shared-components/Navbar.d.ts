interface NavbarProps {
    img?: string;
    menuIcon?: string;
    showIcon: boolean;
}
declare const Navbar: ({ img, menuIcon, showIcon }: NavbarProps) => import("react/jsx-runtime").JSX.Element;
export default Navbar;
